# Canonical repo inventory for {{PROJECT}} (multi-repo projects).
# The source of truth for which repos exist and what each is — and the index to their docs:
# each entry's `location` points to a repo whose own README.md (plus an optional
# ARCHITECTURE.md) is the detailed "about". To orient in a multi-repo project, read this map
# first, then read the README of each repo you'll touch — the same way you read the
# architecture docs before a design change. Update this whenever a repo is created. Tooling/CI
# can read it to stay in sync. See METHOD.md §7.
#
# `location:` is where the repo lives — by default a `Code/*` sibling under the workspace root,
# written workspace-root-relative (e.g. Code/{{PROJECT}}-api/). It MAY instead point OUTSIDE the
# `Code/*` shell — an absolute path or any other arbitrary path — for a repo referenced in place
# where it lives, rather than created as a sibling. setup-workspace.sh uses `location`
# verbatim (clones from `remote` into it, or references it in place when there's no remote), so
# keep `location` and `remote` distinct: a clone URL goes in `remote`, never folded into `location`.
#
# Mono-repo-for-now: the entries below are folders inside the single repo, not separate repos
# yet — this inventory describes the post-split (multi-repo) target. When you do split them
# apart, follow `runbooks/splitting-repos.md`, and delete this note once the repos are real.
#
# Teams: give each repo a `remote:` field (any cloneable git URL). scripts/setup-workspace.sh clones from
# it, and the STEP-number reservation in runbooks/collaboration.md relies on a shared remote
# for prompts/ + this docs hub. Solo with no remotes, it's optional.
#
# `provenance:` records that a repo was **split out** of another one — where it came from, and
# where the two histories part company. Only a split-out repo carries it, and it is written at
# the split (`runbooks/splitting-repos.md`); nothing maintains it afterwards. The shape is in
# the commented example below. `from:` is a historical name and may have no row of its own —
# after a mono-repo-for-now split it never does, because the repo it names was retired; that is
# what `archive_remote:` is for.
#
# `control:` (per row) records whether Throughstone **controls** a repo — whether it may write what
# its needs require into it. `managed` means yes, asked once per repository and never per file;
# `external` means the repo is recorded and referenced but never written into. Control is a state
# that changes over time, not a fact about who created the repo — a repo Throughstone created can be
# handed over, and a repo it never created can be placed under its care.
#
# **A missing value reads as `external`**, because control is a permission and an unanswered
# permission is not granted: the method records the repo and writes nothing into it until someone
# answers. A defaulted row is surfaced rather than silently accepted.
#
# The field is **inert today** — nothing reads it yet. The readers arrive with the repo-control work,
# which adds the ladder that decides what is written into each repo and the checks that enforce it.
# It is written now so the distinction is expressible without reshaping the inventory later.
# (Unrelated to the top-level `throughstone:` key in `.throughstone/manifest.yml`, which versions the
# method itself — different file, different scope.)

repos:
  - name: "{{PROJECT}}-docs"
    location: "Code/{{PROJECT}}-docs/"
    type: docs            # docs | service | library | app | infra | tests
    control: managed
    description: "Documentation hub: architecture, ADRs, standards, registries."

  - name: "prompts"
    location: "prompts/"
    type: docs
    control: managed
    description: "prompts/STEP-index.md roadmap plus archived STEP plans and substep prompts; the build record."

  # Service / app / library repos get added here as the architecture names them and they
  # are stamped from templates/repo-readme-template.md. Give each new repo `control: managed`
  # like the rows above. Example:
  # - name: "{{PROJECT}}-api"
  #   location: "Code/{{PROJECT}}-api/"
  #   type: service
  #   control: managed
  #   remote: "git@example.com:TEAM/{{PROJECT}}-api.git"   # team mode: setup-workspace.sh clones this
  #   description: "..."
  #   provenance:                                          # only on a repo that was split out
  #     from: "{{PROJECT}}-web"                            # the repo this used to be part of
  #     split_on: "2026-01-31"
  #     split_commit: "a1b2c3d"                            # last shared commit — resolves in BOTH repos
  #     archive_remote: "git@example.com:TEAM/{{PROJECT}}.git"   # only when a mono repo was retired
